# src/main/resources/graphql/client/getColecciones.graphql
query GetColecciones {
    colecciones {
        id
        titulo
        descripcion
        criterios{
            tipoCriterio
            valor
            tipoFuente
            fechaInicio
            fechaFin
        }
        algoritmoConsenso
    }
}

